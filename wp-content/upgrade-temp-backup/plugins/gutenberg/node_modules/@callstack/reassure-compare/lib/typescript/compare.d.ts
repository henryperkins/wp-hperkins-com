import type { PerformanceResults } from './types';
type CompareOptions = {
    baselineFile?: string;
    currentFile?: string;
    outputFile?: string;
    outputFormat?: string;
};
/**
 * Main routine.
 *
 * Responsible for loading baseline and current performance results and outputting data in various formats.
 */
export declare function compare({ baselineFile, currentFile, outputFile, outputFormat, }?: CompareOptions): void;
/**
 * Load performance file and parse it to `PerformanceRecord` object.
 */
export declare function loadFile(path: string): PerformanceResults;
export {};
//# sourceMappingURL=compare.d.ts.map