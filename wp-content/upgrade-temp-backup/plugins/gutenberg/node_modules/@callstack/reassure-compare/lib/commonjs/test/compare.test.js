"use strict";

var _compare = require("../compare");
describe('loadFile', () => {
  it('should load results file with header', async () => {
    const results = (0, _compare.loadFile)(`${__dirname}/valid-header.perf`);
    expect(results.metadata).toEqual({
      branch: 'feat/perf-file-validation',
      commitHash: '991427a413b1ff05497a881287c9ddcba7b8de54'
    });
    const entries = Object.keys(results.entries);
    expect(entries).toHaveLength(4);
    expect(entries).toEqual(['Other Component 10', 'Other Component 10 legacy scenario', 'Other Component 20', 'Async Component']);
    expect(results).toMatchSnapshot();
  });
  it('should load results file without header', async () => {
    const results = (0, _compare.loadFile)(`${__dirname}/valid-no-header.perf`);
    expect(results.metadata).toBeUndefined();
    const entries = Object.keys(results.entries);
    expect(entries).toHaveLength(4);
    expect(entries).toEqual(['Other Component 10', 'Other Component 10 legacy scenario', 'Other Component 20', 'Async Component']);
    expect(results).toMatchSnapshot();
  });
  it('should fail for file with invalid JSON structure', async () => {
    expect(() => (0, _compare.loadFile)(`${__dirname}/invalid-json.perf`)).toThrowErrorMatchingSnapshot();
  });
  it('should fail for file with invalid entry', async () => {
    expect(() => (0, _compare.loadFile)(`${__dirname}/invalid-entry.perf`)).toThrowErrorMatchingSnapshot();
  });
});
//# sourceMappingURL=compare.test.js.map