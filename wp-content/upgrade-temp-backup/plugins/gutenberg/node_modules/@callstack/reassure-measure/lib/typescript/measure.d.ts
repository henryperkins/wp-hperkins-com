import * as React from 'react';
import type { MeasureRenderResult } from './types';
export interface MeasureOptions {
    runs?: number;
    dropWorst?: number;
    wrapper?: (node: React.ReactElement) => JSX.Element;
    scenario?: (screen: any) => Promise<any>;
}
export declare function measurePerformance(ui: React.ReactElement, options?: MeasureOptions): Promise<MeasureRenderResult>;
export declare function measureRender(ui: React.ReactElement, options?: MeasureOptions): Promise<MeasureRenderResult>;
